<%- include("../partials/header") %>

<div class="container">
    <header class="jumbotron jumbotron-fluid">
        <div class="container">
            <h1>Welcome Werth Photography!</h1>
            <p>
                <a class="btn btn-primary btn-large" href="/clients/new">Add A New Client</a>
            </p>
        </div>

    </header>

    <div class="row text-center" style="display:flex; flex-wrap: wrap;">
        <div class="col-lg-12">
            <h3>Current Clients</h3>
            <br>
        </div>
    </div>

    <div class="row text-center">
        <% campgrounds.forEach( camp => { %>
            <div class="col-md-3 col-sm-6">
                <div class="thumbnail">
                    <img src="<%= camp.image %>">
                    <div class="caption"></div>
                    <h4><%=camp.date%></h4>
                    <h4><%=camp.name%></h4>
                        <p>
                            <a href="/clients/<%=camp._id%>" class="btn btn-primary">More Info</a>
                        </p>
                </div>
            </div>
        <% }) %>
    </div>

    
    <a href="/">Go Back Home</a>
</div>

<%- include("../partials/footer") %>